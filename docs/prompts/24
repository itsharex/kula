looks like version in usage message and web ui footer is displayed properly only when VERSION file is present on disk, but the information should be contained in the binary

---

1. also add:
-v --version to print only the first line of usage message (program name version - description)
2. add reading OS from /etc/os-release (full name and version) as well as kernel version and cpu architecture (maybe from /proc/kernel ?) version - it's enough to read them once at startup, it doesn't need to be refreshed nor saved into storage

---

I'm running the server

all good, continue and make sure to also include OS, Kernel, Arch in web ui and tui
